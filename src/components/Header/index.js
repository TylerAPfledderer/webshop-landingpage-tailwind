import Hero from "./Hero"
import NavBar from "./NavBar"

const Header = () => {
    return (
        <header>
            <NavBar />
            <Hero />
        </header>
    );
}

export default Header;